<udebs>

<config>
    <revert>5</revert>
    <version>2</version>
    <logging>False</logging>
    <seed>4643</seed>
    <name>life</name>
</config>

<definitions>
    <stats>
        <NBR />
        <LIFE />
    </stats>

    <lists>
        <save />
    </lists>

</definitions>

<maps>
    <map type="diag">
        <dim>
            <x>100</x>
            <y>100</y>
        </dim>
    </map>
</maps>

<entities>

    <!-- Objects -->
    <cell />

    <!-- Metrics -->
    <neighbors>
        <require>DISTANCE.pinf == 1</require>
    </neighbors>

    <!-- Life -->
    <life>
        <require>
            <i>$target.STAT.LIFE == 0</i>
        </require>
        <effect>
            <i>DELAY `($target LIFE REPLACE 1) 0</i>
            <i>all = FILL.$target.neighbors.false</i>
            <i>DELAY `(tick save GETS $all) 0</i>
            <i>DELAY `($all NBR += 1) 0</i>
        </effect>
    </life>

    <autolife>
        <group>life</group>
        <require>$target.STAT.NBR == 3</require>
    </autolife>

    <randomlife>
        <group>life</group>
        <require>DICE.4 == 0</require>
        <effect>tick save GETS $target.LOC</effect>
    </randomlife>

    <!-- Death -->
    <death>
        <require>$target.STAT.LIFE == 1</require>
        <effect>
            <i>DELAY `($target LIFE REPLACE 0) 0</i>
            <i>all = FILL.$target.neighbors.false</i>
            <i>DELAY `(tick save GETS $all) 0</i>
            <i>DELAY `($all NBR -= 1) 0</i>
        </effect>
    </death>

    <autodeath>
        <group>death</group>
        <require>($target.STAT.NBR &lt; 2) or ($target.STAT.NBR &gt; 3)</require>
    </autodeath>

    <!-- Scripts -->
    <init>
        <effect>
            <i>all = (FILL (1 1) empty)</i>
            <i>cell RECRUIT $all</i>
            <i>CAST $all randomlife</i>
        </effect>
    </init>

    <tick>
        <effect>
            <i>all = (dedup $move.STAT.save)</i>
            <i>CAST $all autodeath</i>
            <i>CAST $all autolife</i>
            <i>tick CLEAR save</i>
        </effect>
    </tick>
</entities>
</udebs>
