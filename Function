battleStart(xml_file[str])
battleTick(time[int])
battleRevert(state[int])

getActiveUnits()
getActiveMoves(unit[str])
getStat(unit[str], stat[str])
getMap(command[ all!, y, x, tuple or list ])
getLocation( unit[str] )
getDistance( unit1[str or tuple], unit2[str or tuple], command [ y, x, p1, p2, pinf ]
getGroup( group[ str ] )

controlRecruit( unit, position[tuple] )
controlTravel( caster[str], targetLoc[tuple], command[ travel, x, y, xy ] )
controlStat( unit[str], stat[str], increment[int] )
controlMove( caster[str], target[str, or tuple, or list of targets], move[str], command[ cast!, force])
controlEffect( caster[str], target[str or tuple], move[str] )
controlString( target[str], value to change[str], new value[str] )
controlList( target[str], list to change[str], new entry[str], command[add!, remove]

testBlock( start[ tuple or list ], finish[ tuple or list ], path )
testMove( caster[str], target[str or tuple], move[str] )

interpret( caster[str], target[ID], move[ID], effect/require[str] )

